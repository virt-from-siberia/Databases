CREATE TABLE STUDENTS
(
    ID     NUMBER,
    NAME   VARCHAR2(15),
    COURSE NUMBER NOT NULL
);

SELECT *
FROM STUDENTS;

DROP TABLE STUDENTS;

CREATE TABLE STUDENTS
(
    ID     NUMBER,
    NAME   VARCHAR2(15),
    COURSE NUMBER
        CONSTRAINT ch CHECK (COURSE > 0 AND COURSE < 6)
);

SELECT *
FROM STUDENTS;

INSERT INTO STUDENTS
VALUES (1, 'Aleksey', 2);

INSERT INTO STUDENTS
VALUES (2, 'Ivan', 1);

INSERT INTO STUDENTS
VALUES (1, 'Misha', 1);

SELECT *
FROM STUDENTS;

INSERT INTO STUDENTS
VALUES (1, 'Misha', 6);


UPDATE STUDENTS
SET COURSE = 10
WHERE ID = 1;

UPDATE STUDENTS
SET COURSE = 1
WHERE ID = 1;

SELECT *
FROM STUDENTS;


CREATE TABLE STUDENTS
(
    ID     NUMBER,
    NAME   VARCHAR2(15),
    COURSE NUMBER
);

DROP TABLE STUDENTS;

CREATE TABLE STUDENTS
(
    ID     NUMBER,
    NAME   VARCHAR2(15),
    COURSE NUMBER
);

ALTER TABLE STUDENTS
    MODIFY (ID CONSTRAINT ch CHECK (ID >= 1));

SELECT *
FROM STUDENTS;

ALTER TABLE STUDENTS
    ADD CONSTRAINT ch2 CHECK (COURSE < 20);

SELECT *
FROM STUDENTS;

INSERT INTO STUDENTS
VALUES (1, 'Misha', 6);

INSERT INTO STUDENTS
VALUES (1, 'Lena', 26);

SELECT *
FROM STUDENTS;

DROP TABLE STUDENTS;

CREATE TABLE STUDENTS
(
    ID         NUMBER,
    NAME       VARCHAR2(15),
    COURSE     NUMBER
        CONSTRAINT ch CHECK (COURSE > 0 AND COURSE < 6),
    EMAIL      VARCHAR(15) CHECK (INSTR(EMAIL, '@') > 0),
    FACULTY_ID NUMBER
);

SELECT *
FROM STUDENTS;










