CREATE TABLE FRIENDS
(
    ID      INTEGER PRIMARY KEY,
    NAME    VARCHAR(255) NOT NULL,
    SURNAME VARCHAR(255) NOT NULL
);

SELECT *
FROM FRIENDS;

INSERT INTO FRIENDS
    (SELECT EMPLOYEE_ID, FIRST_NAME, LAST_NAME FROM EMPLOYEES WHERE COMMISSION_PCT IS NOT NULL);

SELECT *
FROM EMPLOYEES;

ALTER TABLE FRIENDS
    ADD (EMAIL VARCHAR(255));

SELECT *
FROM FRIENDS;

ALTER TABLE FRIENDS
    MODIFY (EMAIL DEFAULT 'No Email');


SELECT *
FROM FRIENDS;


INSERT INTO FRIENDS (ID, NAME, SURNAME)
VALUES (777, 'Aleksey', 'Elchin');

SELECT *
FROM FRIENDS
WHERE SURNAME = 'Elchin'


ALTER TABLE FRIENDS RENAME COLUMN ID TO FRIEND_ID;

SELECT *
FROM FRIENDS;

DROP TABLE FRIENDS;

SELECT *
FROM FRIENDS;

CREATE TABLE FRIENDS
(
    ID       INTEGER PRIMARY KEY,
    NAME     VARCHAR(255),
    SURNAME  VARCHAR(255),
    EMAIL    VARCHAR(255),
    SALARY   INTEGER DEFAULT 0,
    CITY     VARCHAR(255),
    BIRTHDAY DATE    DEFAULT SYSDATE

);

SELECT *
FROM FRIENDS;

INSERT INTO FRIENDS (ID, NAME, SURNAME, EMAIL, SALARY, BIRTHDAY)
VALUES (1, 'Aleksey', 'Elchin', 'alonso85@mail.ru', 270000, '02-10-1985');


SELECT *
FROM FRIENDS;


UPDATE FRIENDS
SET CITY = 'Chita'
WHERE ID = 1;

SELECT *
FROM FRIENDS;

INSERT INTO FRIENDS (ID, NAME, SURNAME, EMAIL)
VALUES (2, 'Ivan', 'Elchin', 'vano121@mail.ri');

SELECT *
FROM FRIENDS;

COMMIT;

COMMIT;

ALTER TABLE FRIENDS
    DROP COLUMN SALARY;

SELECT *
FROM FRIENDS;

ALTER TABLE FRIENDS
    DROP COLUMN EMAIL;

ALTER TABLE FRIENDS
    DROP COLUMN BIRTHDAY;


SELECT *
FROM FRIENDS;

TRUNCATE TABLE FRIENDS;

SELECT *
FROM FRIENDS;

DROP TABLE FRIENDS;

SELECT *
FROM FRIENDS;












