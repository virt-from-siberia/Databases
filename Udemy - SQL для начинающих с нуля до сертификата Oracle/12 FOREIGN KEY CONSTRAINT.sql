SELECT *
FROM EMPLOYEES;

SELECT *
FROM DEPARTMENTS;

DROP TABLE FACULTIES;

CREATE TABLE FACULTIES
(
    ID   NUMBER PRIMARY KEY,
    NAME VARCHAR2(15)
);


CREATE TABLE STUDENTS
(
    ID         NUMBER,
    NAME       VARCHAR2(15),
    COURSE     NUMBER NOT NULL,
    FACULTY_ID INTEGER
);

DROP TABLE STUDENTS;

CREATE TABLE STUDENTS
(
    ID         NUMBER,
    NAME       VARCHAR2(15),
    COURSE     NUMBER NOT NULL,
    FACULTY_ID INTEGER
        CONSTRAINT st_faculty_fk REFERENCES FACULTIES (ID) ON DELETE CASCADE
);

INSERT INTO FACULTIES
VALUES (1, 'CS');

INSERT INTO FACULTIES
VALUES (2, 'Marketing');

COMMIT;

SELECT *
FROM STUDENTS;

SELECT *
FROM FACULTIES;

INSERT INTO STUDENTS
VALUES (1, 'Aleksey', 3, 1);

SELECT *
FROM STUDENTS;











