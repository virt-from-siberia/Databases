DELETE
FROM NEW_EMPS;

SELECT *
FROM NEW_EMPS;

INSERT INTO NEW_EMPS (SELECT EMPLOYEE_ID, FIRST_NAME, HIRE_DATE, JOB_ID FROM EMPLOYEES);
COMMIT;

SELECT *
FROM NEW_EMPS;

INSERT INTO NEW_EMPS
VALUES (777, 'ALEX', SYSDATE, 'IT_PROG');

SELECT *
FROM NEW_EMPS
WHERE EMP_ID = '777';

DELETE
FROM NEW_EMPS
WHERE EMP_ID = '777';

SELECT *
FROM NEW_EMPS
WHERE EMP_ID = '777';

ROLLBACK;

SELECT *
FROM NEW_EMPS
WHERE EMP_ID = '777';