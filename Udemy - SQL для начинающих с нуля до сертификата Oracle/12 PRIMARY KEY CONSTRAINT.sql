DROP TABLE STUDENTS;

CREATE TABLE STUDENTS
(
    ID         NUMBER
        CONSTRAINT st_id_primary_key PRIMARY KEY,
    NAME       VARCHAR2(15),
    COURSE     NUMBER DEFAULT 3,
    FACULTY_ID INTEGER
);

SELECT *
FROM STUDENTS;

INSERT INTO STUDENTS
VALUES (1, 'Aleksey', NULL, 7);

SELECT *
FROM STUDENTS;

INSERT INTO STUDENTS
VALUES (2, 'Ivan', NULL, 7);

SELECT *
FROM STUDENTS;

INSERT INTO STUDENTS
VALUES (3, 'Zaur', NULL, 7);

SELECT *
FROM STUDENTS;


DROP TABLE FACULTIES;

CREATE TABLE FACULTIES
(
    ID   NUMBER
        CONSTRAINT fc_id_primary_key PRIMARY KEY,
    NAME VARCHAR2(15)
);

INSERT INTO FACULTIES
VALUES (1, 'CS');

INSERT INTO FACULTIES
VALUES (7, 'ECONOMY');

SELECT *
FROM FACULTIES;


SELECT *
FROM STUDENTS ST
         JOIN FACULTIES FA ON (ST.FACULTY_ID = FA.ID);

SELECT *
FROM STUDENTS;

DROP TABLE STUDENTS;

CREATE TABLE STUDENTS
(
    ID         NUMBER,
    NAME       VARCHAR2(15),
    COURSE     NUMBER DEFAULT 3,
    FACULTY_ID INTEGER
);

SELECT *
FROM STUDENTS;

DROP TABLE STUDENTS;

CREATE TABLE STUDENTS
(
    ID         NUMBER,
    NAME       VARCHAR2(15),
    COURSE     NUMBER DEFAULT 3,
    FACULTY_ID INTEGER,
    CONSTRAINT pk PRIMARY KEY (ID, NAME)
);

SELECT *
FROM STUDENTS;

DROP TABLE STUDENTS;

CREATE TABLE STUDENTS
(
    ID         NUMBER,
    NAME       VARCHAR2(15),
    COURSE     NUMBER DEFAULT 3,
    FACULTY_ID INTEGER
);


ALTER TABLE STUDENTS
    MODIFY (ID PRIMARY KEY);


SELECT *
FROM STUDENTS;














