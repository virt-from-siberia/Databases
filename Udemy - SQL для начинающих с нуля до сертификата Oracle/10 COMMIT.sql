SELECT *
FROM EMPLOYEES;

COMMIT;

DELETE
FROM NEW_EMPS
WHERE NAME = 'Pat';
COMMIT;

INSERT INTO NEW_EMPS
VALUES (1000, 'Fedor', SYSDATE, 'IT_PROG');
COMMIT;

SELECT *
FROM NEW_EMPS
WHERE NAME = 'Fedor';

UPDATE NEW_EMPS
SET EMP_ID = 300
WHERE EMP_ID = 100;

SELECT *
FROM NEW_EMPS
WHERE EMP_ID = 300;

SELECT *
FROM NEW_EMPS
WHERE EMP_ID = 201;

